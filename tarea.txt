👑 Rol 1: El Jefe (Multiprocessing y Ensamblaje)
Tu objetivo es leer el archivo gigante a trozos sin saturar el ordenador, repartir los trozos, y sumar los resultados finales.

Importa las librerías: Escribe import pandas as pd y from multiprocessing import Pool.

Prepara la lectura a trozos: Usa la función lector = pd.read_csv("datos_prueba.csv", chunksize=500000). Esto hace que Pandas no lea el archivo entero, sino que te dé paquetes de medio millón de filas.

Crea la función "Trabajador": Define una función def procesar_trozo(df): que recibirá un paquete de datos. Dentro de esta función, llamarás a las funciones que están programando tus 4 compañeros y guardarás todos sus resultados en un solo diccionario.

Activa el Multiprocessing: En el bloque if __name__ == '__main__':, crea un "Pool" de procesos (ej: with Pool(procesos=4) as p:). Usa p.map(procesar_trozo, lector) para enviar todos los trozos a la vez.

Suma todo al final: Recibirás una lista con los diccionarios de cada trozo. Tienes que crear variables totales (ej. salario_total = 0) y hacer un bucle para sumar lo que te ha devuelto cada trozo.

Calcula las medias: Una vez sumado todo el archivo gigante, divide el salario_total entre total_personas para sacar la media. Haz lo mismo con la edad.

🧮 Rol 2: Calculador Base (Sumas simples y Paro)
Tu objetivo es extraer los números brutos para que el Jefe pueda hacer las medias al final.

Crea tu función principal: Escribe def sacar_basicos(df):

Suma los salarios: Crea una variable suma_salarios = df['salario'].sum() (asegúrate del nombre exacto de la columna).

Suma las edades: Crea una variable suma_edades = df['edad'].sum().

Cuenta las personas: Averigua cuántas filas hay en este trozo con total_gente = len(df).

Cuenta los parados: Filtra los que no tienen trabajo. Por ejemplo: parados = (df['estado'] == 'parado').sum().

Devuelve los datos: Al final de tu función escribe return {"salarios": suma_salarios, "edades": suma_edades, "personas": total_gente, "parados": parados}. Se lo pasas al Jefe.

🕵️ Rol 3: Detective de Nombres (Cifrado César)
Tu objetivo es buscar a "Fausto" y a los tocayos de vuestro grupo sin perder tiempo descifrando 10 millones de filas.

Cifra las palabras clave a mano (fuera del código principal): Si el código de la imagen usa un desplazamiento de 3, "FAUSTO" cifrado será otra palabra (ej. "IDXVWR"). Averigua cuál es esa palabra cifrada exacta. Haz lo mismo con vuestros 5 nombres reales.

Crea tu función principal: Escribe def buscar_nombres(df):.

Busca a Fausto: Escribe num_faustos = df['nombre'].isin(['Idxvwr']).sum() (pon la palabra cifrada correcta).

Busca a los tocayos: Crea una lista con vuestros 5 nombres cifrados: mis_nombres = ['Cifrado1', 'Cifrado2', ...]. Luego cuenta cuántos hay: num_tocayos = df['nombre'].isin(mis_nombres).sum().

Devuelve los datos: Escribe return {"faustos": num_faustos, "tocayos": num_tocayos}. Se lo pasas al Jefe.

🏙️ Rol 4: Geógrafo (Ciudades y Edades)
Tu objetivo es sumar el dinero agrupando por ciudad y por bloques de edad.

Crea tu función principal: Escribe def agrupar_datos(df):.

Agrupa por ciudades: Escribe dinero_ciudades = df.groupby('ciudad')['salario'].sum().to_dict(). Esto suma todo el dinero de Madrid, Barcelona, etc., en ese trocito y lo convierte en un diccionario.

Crea los rangos de edad: Usa la función pd.cut. Escribe rangos = pd.cut(df['edad'], bins=range(0, 105, 5)). Esto clasifica a cada persona en cajones de 5 años (0-4, 5-9, 10-14...).

Agrupa por rangos: Escribe dinero_edades = df.groupby(rangos)['salario'].sum().to_dict().

Devuelve los datos: Escribe return {"ciudades": dinero_ciudades, "edades": dinero_edades}. Pásaselo al Jefe. (Nota: El Jefe tendrá la tarea de sumar los diccionarios de todos los trozos y sacar el Top 3 final).

🗳️ Rol 5: Analista Político (Elecciones)
Tu objetivo es contar papeletas y decidir quién gobierna. Tienes que hacer dos funciones distintas.

Función para contar en cada trozo: Escribe def contar_votos(df):. Dentro, pon votos = df['partido'].value_counts().to_dict() y devuelve eso con un return. Esto se lo pasas al Jefe para que lo meta en su bucle.

Función para la noche electoral (Para el final del todo): Crea otra función separada def calcular_ganador(votos_totales_diccionario):. El Jefe llamará a esta función al final del todo, pasándole la suma de tooooodos los votos del país.

Calcula la mayoría absoluta: En esa segunda función, suma todos los votos para saber el 100%. Divide entre 2 y suma 1 para saber dónde está la mayoría absoluta.

Haz la coalición: Ordena el diccionario de votos de mayor a menor. Ve sumando los votos del primer partido, luego del segundo, luego del tercero... hasta que pases la mayoría absoluta. Devuelve los nombres de los partidos que has tenido que sumar.